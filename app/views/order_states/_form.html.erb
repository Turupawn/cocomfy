<%= form_with(model: order_state, local: true) do |form| %>
  <% if order_state.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(order_state.errors.count, "error") %> prohibited this order_state from being saved:</h2>

      <ul>
      <% order_state.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :name %>
    <%= form.text_field :name %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
